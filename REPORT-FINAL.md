# Отчёт о готовности к тестированию (AI Chat Lend)

Проведен глубокий анализ кодовой базы frontend и backend. Текущее состояние приложения визуально выглядит как готовое, однако под капотом есть критические блокирующие недоработки, из-за которых **нельзя пускать реальных пользователей на тестирование**. 

Ниже представлен список того, что необходимо обязательно доработать.

---

## 🛑 1. Критические блокеры (Critical Blockers)

### 1.1. Полное отсутствие AI (LLM) ответов в чате!
- **Проблема (Backend):** В файле [backend/src/routes/ws.ts](file:///c:/dev/ai-chat-lend/backend/src/routes/ws.ts) есть обработчик входящих сообщений. Однако при получении сообщения от пользователя (`role === 'user'`) сервер **ТОЛЬКО** сохраняет его в БД и пересылает админам. Функция [handleFreeChat](file:///c:/dev/ai-chat-lend/backend/src/services/chatService.ts#5-122) (которая вызывает RouterAI и Qdrant) **нигде не вызывается**. 
- **Проблема (Frontend):** В [frontend/src/store/chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts) при `chatState === 'FREE_CHAT'` вшит хардкод-ответ: *"Пока я работаю в демо-режиме, но скоро смогу..."*. Сообщения пользователя даже не передаются на бэкенд для запроса к ИИ.
- **Итог:** Бот сейчас — это просто жестко запрограммированная кнопка-калькулятор. Никакого AI нет.

### 1.2. Поломка динамической воронки (Конфликт Фазы 11)
- **Проблема:** В Фазе 11 была добавлена возможность редактировать шаги воронки через админку (вкладка [BotPersonality.tsx](file:///c:/dev/ai-chat-lend/frontend/src/pages/admin/tabs/BotPersonality.tsx), где тенант может менять вопросы, тип ввода и логику `skipIf`). Однако логика расчета сметы внутри виджета ([chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts), строки 288-360) **жестко привязана к ID конкретных шагов**: `area`, `rooms`, `repairType`, `design`.
- **Итог:** Если тенант удалит шаг с ID `area`, или добавит новый шаг вместо него, калькулятор сметы сломается и выдаст `NaN`. Нужно либо жестко закрепить базовые системные шаги (не разрешать их удалять), либо переписать калькулятор на абстрактную логику.

### 1.3. Сбор контактов (Lead Capture) незакончен
- **Проблема:** В [GEMINI.md](file:///c:/dev/ai-chat-lend/GEMINI.md) указано: *"пользователь выбирает канал отправки PDF - telegram / whatsapp / email"*. 
- **Реальность:** В [chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts) жестко зашит единственный сценарий проверки: *"Оставьте ваш телефон 👇"*. Пользователь не может выбрать мессенджер, и метод [submitLead('phone', text)](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts#90-106) всегда шлет тип `phone`.

---

## ⚠️ 2. Средние и мелкие недоработки (UX/UI & Bugs)

### 2.1. Баг отображения `NaN%` в Dashboard.tsx
- **Проблема:** На странице аналитики админа [Dashboard.tsx](file:///c:/dev/ai-chat-lend/frontend/src/pages/admin/Dashboard.tsx) процент выбора сегментов рассчитывается как `Math.round((seg.value / total) * 100)`. Если по сегментам нет данных (например, для нового тенанта, где `total === 0`), это деление на 0 выдаёт `NaN%` в интерфейсе. 

### 2.2. Заглушки цен (Fallback Rates) в [chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts)
- **Проблема:** Если тенант не настроил свои `segments` и матрицы цен, виджет использует хардкод `DEFAULT_RATES` из [chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts) (вилки от 5000 до 50000 руб.). 
- **Решение:** При создании нового тенанта в его БД должна прошиваться базовая (дефолтная) матрица, чтобы фронтенд никогда не полагался на хардкод, а всегда брал ставки по API настройки тенанта.

### 2.3. Таблица `dialog_labels` для Phase 12
- **Проблема:** В [TASK_PHASE12.md](file:///c:/dev/ai-chat-lend/TASK_PHASE12.md) указано, что таблица `dialog_labels` *"уже есть"*. Но скрипт миграций [backend/src/db/migrate.ts](file:///c:/dev/ai-chat-lend/backend/src/db/migrate.ts) не содержит команды создания этой таблицы. При попытке разработать функционал тегирования (Labels) сервер упадет из-за отсутствия отношения в БД.

---

## 📝 Резюме (Что делать дальше?)
Перед тем как давать приложение тестерам, необходимо:
1. Подключить вызов [handleFreeChat](file:///c:/dev/ai-chat-lend/backend/src/services/chatService.ts#5-122) в [ws.ts](file:///c:/dev/ai-chat-lend/backend/src/routes/ws.ts) и убрать заглушку "демо-режим" из [chatStore.ts](file:///c:/dev/ai-chat-lend/frontend/src/store/chatStore.ts).
2. Заблокировать удаление системных шагов (`area`, `rooms`, `repairType`, `design`) в редакторе воронки [BotPersonality.tsx](file:///c:/dev/ai-chat-lend/frontend/src/pages/admin/tabs/BotPersonality.tsx), чтобы не ломать калькулятор.
3. Добавить UI-кнопки выбора мессенджера (TG, WA, Почта) на этапе сбора лида (`LEAD_CAPTURE`).
4. Починить деление на ноль в дашборде.
