# Спецификация админ-панели AI-чат-лендинга

## Общая информация

**Назначение**: Центр управления чат-ботом, мониторинг конверсий, настройка поведения AI-эксперта Макса, управление базой знаний и ценами, анализ диалогов, интеграции с внешними сервисами.

**Архитектура**: Web-приложение с отдельным роутингом `/admin`, авторизация по логину/паролю, роли: администратор, менеджер (просмотр диалогов + подключение к чату), контент-менеджер (редактирование базы знаний).

---

## 1. Раздел: Дашборд

### Назначение
Главный экран с агрегированными метриками эффективности лендинга и воронки конверсии.

### Метрики

| Метрика | Описание | Источник данных |
|---------|----------|-----------------|
| **Визиты на лендинг** | Уникальные открытия страницы лендинга | Таблица `sessions`, поле `created_at` |
| **Открыт чат** | Нажали кнопку «Начать бесплатно» или выбрали быстрый вопрос | `sessions.status = 'chat_opened'` |
| **Начата воронка сметы** | Выбрали «Рассчитать стоимость» | `sessions.status = 'estimate_started'` |
| **Завершена воронка** | Прошли все 8 шагов, получили вилку цен | `sessions.status = 'estimate_completed'` |
| **Оставлен контакт** | Ввели телефон/email/Telegram | Наличие записи в `leads` для session_id |
| **Конверсия лендинг → лид** | Процент от визитов до лида | `(leads.count / sessions.count) * 100` |
| **Среднее время диалога** | Медианное время от открытия чата до последнего сообщения | `median(last_message.created_at - first_message.created_at)` |

### Визуализация
- Карточки с ключевыми метриками (большие цифры + динамика за период)
- График воронки конверсии (Sankey diagram): Визиты → Открыт чат → Начата воронка → Завершена воронка → Оставлен контакт
- График по временной шкале (выбор периода: день/неделя/месяц)
- Фильтры: диапазон дат, источник трафика (UTM-метки), устройство (desktop/mobile)

### Интеграция с Яндекс Метрикой
События, которые передаются как цели в Яндекс Метрику (настраивается в разделе «Интеграции»):
- `chat_opened` — при нажатии «Начать»
- `estimate_started` — при выборе «Рассчитать стоимость»
- `estimate_completed` — при завершении воронки и показе вилки
- `lead_created` — при сохранении контакта

**Техническая реализация**: JavaScript API Яндекс Метрики, вызовы `ym(counter_id, 'reachGoal', 'event_name')` на клиенте + дублирование через Measurement Protocol на backend.

---

## 2. Раздел: Настройка бота

### 2.1. Вкладка «Личность»

**Назначение**: Определение характера и внешнего вида AI-эксперта.

**Поля настройки**:

| Параметр | Тип поля | Описание |
|----------|----------|----------|
| **Имя AI-эксперта** | Текст (макс. 50 символов) | По умолчанию «Макс», влияет на обращение в диалогах |
| **Аватар** | Загрузка изображения | Формат JPG/PNG, рекомендуемый размер 256×256 px, отображается в чате |
| **Тон общения** | Выбор из списка | Варианты: Профессиональный / Дружелюбный / Нейтральный. Влияет на formality промпта |
| **Язык** | Выбор из списка | Русский / Английский. Определяет язык всех сообщений бота |
| **Приветственное сообщение** | Textarea, Markdown | Редактируемый текст первого сообщения при открытии чата. По умолчанию: «Привет! Я Макс — AI-эксперт по ремонту квартир в Челябинске...» |
| **Быстрые кнопки на старте** | Список с inline-редактированием | Кнопки под приветствием (добавить/удалить/переименовать/изменить порядок drag-and-drop). Максимум 6 кнопок. Каждая кнопка: текст (50 символов) + emoji (опционально) + действие (начать воронку / задать вопрос из базы знаний / произвольный текст) |

**Логика применения**:
- Изменения в полях «Имя», «Аватар», «Тон» сразу влияют на новые сессии.
- Приветственное сообщение и кнопки обновляются мгновенно для всех клиентов (хранятся в конфиге, не в system prompt).
- При изменении тона пересобирается system prompt для RouterAI.

---

### 2.2. Вкладка «Сегменты ремонта»

**Назначение**: Детальное описание каждого ценового сегмента для пояснения пользователям в воронке и при свободных вопросах.

**Структура**: 4 редактируемые карточки (эконом / стандарт / комфорт / премиум).

**Поля для каждой карточки**:

| Поле | Тип данных | Пример |
|------|------------|--------|
| **Название сегмента** | Текст (не редактируется) | «Эконом» |
| **Краткое описание** | Textarea, до 300 символов | «Базовый ремонт с сертифицированными материалами эконом-класса. Простые решения без архитектурных изысков» |
| **Что входит** | Textarea, Markdown, список | «- Штукатурка/шпаклёвка стен\n- Покраска или обои\n- Ламинат 32 класс\n- Сантехника и электрика стандарт» |
| **Примерный диапазон цен** | Два поля: от / до, руб/м² | От: 15000, До: 22000 |
| **Типичные материалы** | Текст (опционально) | «Knauf, Ceresit CT127, Tarkett» |

**Использование данных**:
1. **В воронке**: При клике на кнопку «Что входит в каждый?» на шаге 7 (выбор сегмента) пользователю показывается модальное окно с описанием всех 4 сегментов.
2. **В свободных диалогах**: Если пользователь спрашивает «Чем отличается комфорт от стандарта?», Макс извлекает информацию из этих описаний через контекст промпта.

**Техническая реализация**: Данные хранятся в таблице `segments` (PostgreSQL), при изменении пересобирается JSON-конфиг для system prompt RouterAI.

---

### 2.3. Вкладка «Поведение»

**Назначение**: Управление логикой эскалации, правилами триггеров и обязательными дисклеймерами.

**Настройки**:

| Параметр | Тип | Описание |
|----------|-----|----------|
| **Триггерные слова для эскалации** | Список (редактируемый) | Слова/фразы, при наличии которых в сообщении пользователя бот предлагает переключение на менеджера. Примеры: «дорого», «не устраивает», «хочу говорить с человеком», «менеджер». Каждое слово — отдельная строка. Добавление/удаление через UI |
| **Макс. число сообщений без лид-CTA** | Число (от 3 до 20) | После N сообщений пользователя (вне воронки) без запроса контакта бот мягко предлагает: «Хотите, отправлю детальную смету на вашу почту?». По умолчанию: 5 |
| **Обязательные дисклеймеры в смете** | Textarea, Markdown | Текст, который добавляется в конец PDF и текстового результата. Пример: «Данная смета является ориентировочной. Точная стоимость определяется после бесплатного замера. Не является публичной офертой» |
| **Текст уведомления о 72-часовом TTL PDF** | Textarea, до 200 символов | Сообщение, которое показывается пользователю при отправке PDF. Пример: «Внимание: ссылка на PDF активна 72 часа. Сохраните файл, если понадобится позже» |

**Логика применения**:
- **Триггеры**: При каждом сообщении пользователя backend проверяет наличие триггерных слов (регистронезависимо). Если обнаружено — в следующий ответ Макса вставляется предложение: «Могу подключить нашего специалиста — он ответит лично. Хотите?» + кнопки Да/Нет.
- **Лид-CTA**: Счётчик сообщений хранится в `sessions.message_count`. При превышении лимита — автоматическая вставка CTA в ответ.
- **Дисклеймеры**: Прошиваются в HTML-шаблон PDF и в финальное сообщение с вилкой.

---

### 2.4. Вкладка «База знаний (RAG)»

**Назначение**: Управление контентом для ответов Макса на свободные вопросы о ремонте, компании, технологиях.

**Функции**:

#### 2.4.1. Загрузка документов

| Действие | Описание |
|----------|----------|
| **Загрузить файлы** | Поддерживаемые форматы: PDF, DOCX, TXT. Макс. размер 10 MB на файл. Типы контента: прайсы, регламенты работ, гарантийные условия, FAQ компании, СНиПы |
| **Список загруженных документов** | Таблица: название файла, дата загрузки, размер, статус индексации (индексируется / готово / ошибка), кнопка «Удалить» |

**Процесс индексации**:
1. Файл загружается на сервер.
2. Парсер извлекает текст (PDF → PyPDF2, DOCX → python-docx).
3. Текст разбивается на chunks по 512 токенов с overlap 50 токенов.
4. Каждый chunk эмбеддится через RouterAI (модель text-embedding-3-small или аналог).
5. Векторы сохраняются в векторную БД (Qdrant или Supabase pgvector).
6. Статус меняется на «Готово».

#### 2.4.2. Редактор статей (Markdown)

**Назначение**: Написание ответов на типовые вопросы вручную.

**UI**:
- Список статей (заголовок + превью).
- Кнопка «Создать статью».
- Форма: Заголовок (вопрос), Текст ответа (Markdown-редактор), Ключевые слова (теги для поиска).
- При создании статья также эмбеддится и индексируется в RAG.

**Примеры статей**:
- «Сколько длится ремонт под ключ двухкомнатной квартиры?»
- «Чем частный мастер отличается от компании?»
- «Какие гарантии даёт компания?»

#### 2.4.3. «Белые пятна»

**Назначение**: Список вопросов пользователей, на которые Макс не нашёл ответа в базе знаний (similarity score < 0.7).

**UI**:
- Таблица: вопрос пользователя, дата, session_id, кнопка «Заполнить ответ».
- При клике открывается форма редактора статей с предзаполненным вопросом.
- После сохранения вопрос удаляется из списка белых пятен.

**Автоматическое пополнение**:
- Когда RAG-поиск не возвращает релевантного результата, вопрос автоматически добавляется в таблицу `knowledge_gaps`.

#### 2.4.4. Управление индексом

| Элемент | Описание |
|---------|----------|
| **Дата последнего обновления индекса** | Метка времени последней индексации |
| **Кнопка «Переиндексировать всё»** | Пересоздаёт все эмбеддинги для всех документов и статей. Используется при смене модели эмбеддингов или изменении стратегии chunking. Процесс занимает 5–15 минут, статус показывается в реальном времени |

---

### 2.5. Вкладка «Справочник цен»

**Назначение**: Управление ценами на виды работ по сегментам. Источник данных для движка расчёта сметы.

**Структура данных**: Таблица `price_matrix`.

**UI**:
- **Таблица** с inline-редактированием:
  - Столбцы: Вид работы | Ед. изм. | Эконом (min–max) | Стандарт (min–max) | Комфорт (min–max) | Премиум (min–max) | Категория
  - Строки: все виды работ (минимум 30–40 позиций: штукатурка, шпаклёвка, покраска, укладка плитки, монтаж электрики и т.д.)
  - Inline-редактирование: двойной клик по ячейке → изменение значения → Enter для сохранения
  - Фильтрация по категории (Стены / Пол / Потолок / Электрика / Сантехника)
- **Кнопка «Добавить вид работы»**: открывает форму с полями (название, единица измерения, категория, цены по сегментам).
- **Дата последнего обновления**: метка времени, автоматически обновляется при любом изменении цен.
- **Кнопка «Обновить дату актуальности»**: при нажатии обновляет метку `price_updated_at` в конфиге. Эта дата показывается пользователям в чате в формате «Цены актуальны на февраль 2026».

**Логика применения**:
- Изменения цен вступают в силу мгновенно для новых расчётов.
- При обновлении даты актуальности в текстовом ответе Макса появляется: «Расчёт основан на актуальных ценах Челябинска на [месяц год]».

---

## 3. Раздел: Диалоги

**Назначение**: Мониторинг всех взаимодействий пользователей с ботом, анализ качества лидов, возможность подключения менеджера к активному диалогу.

### 3.1. Список диалогов

**UI**: Таблица с пагинацией (20 записей на страницу).

**Столбцы таблицы**:

| Столбец | Описание |
|---------|----------|
| **Дата/время** | Время создания сессии |
| **Источник** | UTM-метки (utm_source, utm_medium, utm_campaign) или «Прямой заход» |
| **Статус** | Лид (зелёный) / В процессе (жёлтый) / Закрыт без лида (серый) |
| **Площадь** | Если воронка пройдена — площадь квартиры |
| **Тип ремонта** | Косметический / Капитальный / Под ключ |
| **Сегмент** | Эконом / Стандарт / Комфорт / Премиум |
| **Итоговая вилка** | Если расчёт выполнен: «от X до Y руб.» |
| **Контакт** | Телефон/email, если оставлен (маскированный: +7 912 *** ** 45) |
| **Оценка** | Иконки: ✅ / ❌ / ⚠️ / пусто (ставится вручную при просмотре) |

**Фильтры** (сверху таблицы):

- **По дате**: диапазон (от / до), пресеты (сегодня / неделя / месяц / всё время)
- **По статусу**: Все / Лид / Не лид / В процессе
- **По сегменту**: Все / Эконом / Стандарт / Комфорт / Премиум
- **По источнику**: выпадающий список уникальных utm_source
- **Поиск**: по телефону, email или фразе из диалога (full-text search по таблице `messages`)

**Сортировка**: по любому столбцу (↑ / ↓).

---

### 3.2. Детальный просмотр диалога

**UI**: Модальное окно или отдельная страница.

#### 3.2.1. История переписки

- **Внешний вид**: интерфейс как в мессенджере.
- **Элементы**:
  - Сообщения бота: слева, аватар Макса, серый фон пузыря.
  - Сообщения пользователя: справа, белый фон пузыря.
  - Сообщения менеджера (если подключался): слева, имя менеджера вместо аватара Макса, синий фон.
  - Временные метки под каждым сообщением.
  - Если были кнопки — показываются как неактивные элементы с выделением выбранного варианта.

#### 3.2.2. Метаданные сессии

**Панель справа или над диалогом**:

| Параметр | Пример значения |
|----------|-----------------|
| **Session ID** | `a3f5d2c1-8b4e-4f12-9a7c-6d8e2f1a3b5c` |
| **Дата начала** | 26 февраля 2026, 18:34 |
| **Длительность диалога** | 8 минут 42 секунды |
| **Устройство** | iPhone 14 Pro, iOS 17.2, Mobile Safari |
| **UTM-метки** | source: yandex_direct, medium: cpc, campaign: chelya_remont_feb |
| **Прогресс воронки** | Шаги: 8/8 (завершена), время на каждом шаге: 1) 12с, 2) 8с, 3) 15с... |
| **Статус онлайн** | Индикатор: 🟢 Пользователь онлайн / 🔴 Закрыл вкладку — недоступен |
| **IP-адрес** | 93.185.X.X (Челябинск, RU) |

**Логика определения статуса онлайн**:
- WebSocket-соединение активно → статус «Онлайн».
- Соединение разорвано или ping без ответа → статус «Недоступен».
- Обновление каждые 5 секунд.

#### 3.2.3. Кнопка «Подключиться»

**Назначение**: Передача управления диалогом менеджеру (human takeover).

**Логика работы**:
- **Если пользователь онлайн**:
  - Кнопка активна.
  - При нажатии:
    1. WebSocket-канал переключается в режим `manager_takeover`.
    2. Пользователю показывается сообщение: «Подключился специалист [Имя менеджера]. Сейчас отвечу».
    3. Макс перестаёт отвечать автоматически.
    4. Менеджер печатает сообщения через админку — они отправляются в чат пользователя в реальном времени.
    5. В списке диалогов сессия помечается как «Подключён менеджер» (оранжевый бейдж).
- **Если пользователь недоступен (закрыл вкладку)**:
  - Кнопка неактивна (серая).
  - Tooltip при наведении: «Пользователь недоступен. Подключение невозможно».
  - Предупреждение при попытке клика: «Пользователь закрыл вкладку. Вы можете оставить комментарий для последующего контакта».

**UI интерфейса менеджера** (в режиме подключения):
- Поле ввода сообщения внизу экрана.
- История диалога сверху.
- Кнопка «Завершить подключение» → возвращает управление боту.

#### 3.2.4. Ручная оценка диалога

**Назначение**: Классификация лида для обучения системы.

**Варианты оценки**:

| Иконка | Название | Описание |
|--------|----------|----------|
| ✅ | Качественный лид | Целевой пользователь, реальный интерес к ремонту, контакт оставлен, готов к звонку |
| ❌ | Нецелевой | Случайный клик, конкурент, тестирование, некорректные данные |
| ⚠️ | Требует доработки бота | Пользователь ушёл из-за непонятного вопроса или некорректного ответа Макса |
| 🔄 | Повторное обращение | Пользователь уже был в базе, вернулся для уточнения |

**Использование**:
- Данные оценок используются в модуле самообучения для выявления паттернов успешных и неуспешных диалогов.
- Фильтр в списке диалогов по оценке.

---

## 4. Раздел: Самообучение

**Назначение**: Автоматический анализ эффективности бота и генерация рекомендаций по улучшению промптов и логики воронки.

### 4.1. График конверсии по неделям

**UI**: Линейный график (ось X — недели, ось Y — процент конверсии).

**Метрики**:
- Конверсия «Визит → Начало воронки» (%).
- Конверсия «Начало воронки → Завершение воронки» (%).
- Конверсия «Завершение воронки → Лид» (%).

**Функции**:
- Наведение на точку графика показывает точные значения и количество сессий.
- Вертикальные маркеры для обозначения изменений промпта или конфигурации (с подписями: «Изменён вопрос на шаге 3», «Обновлены описания сегментов»).

---

### 4.2. Топ «точек отвала»

**Назначение**: Визуализация шагов воронки, на которых пользователи чаще всего прекращают диалог.

**UI**: Таблица или столбчатая диаграмма.

| Шаг воронки | Описание | % отвала | Количество |
|-------------|----------|----------|------------|
| Шаг 3: Состояние квартиры | Новостройка / Вторичка | 32% | 124 сессии |
| Шаг 7: Ценовой сегмент | Эконом / Стандарт / Комфорт / Премиум | 18% | 67 сессий |
| Шаг 1: Площадь квартиры | Ввод числа | 12% | 45 сессий |

**Логика расчёта**:
- Отвал = сессии со статусом `dropped_at_step_N` (пользователь не отправил ответ на шаг N и закрыл вкладку или ушёл без действий > 5 минут).

---

### 4.3. Очередь предложений от LLM-агента

**Назначение**: Автоматическая генерация рекомендаций по улучшению на основе анализа диалогов.

**UI**: Список карточек, каждая карточка — одно предложение.

**Структура карточки**:

| Элемент | Описание |
|---------|----------|
| **Тип проблемы** | «Высокий отвал на шаге» / «Низкая конверсия CTA» / «Частые вопросы без ответа» / «Триггер эскалации» |
| **Описание проблемы** | Пример: «На шаге 3 (Состояние квартиры) пользователи уходят в 32% случаев. Вероятная причина: неоднозначность вариантов "Вторичка (косметика)" и "Вторичка (капремонт)"» |
| **Рекомендация** | Конкретное предложение: «Изменить формулировку вопроса: "Какой ремонт был сделан ранее?" Варианты: "Не было / Косметический (обои, покраска) / Старый капитальный (> 10 лет)"» |
| **Обоснование** | «Анализ 89 диалогов показал, что 76% пользователей повторно кликали на кнопки или писали "не понял разницу"» |
| **Кнопки действий** | **Применить** / **Отклонить** / **Изменить вручную** |

**Процесс генерации**:
1. Еженедельно (или по требованию) LLM-агент через RouterAI анализирует выборку диалогов (200–500 последних).
2. Агент получает промпт с инструкцией: найти паттерны (успешные и неуспешные), выделить слабые места, предложить 3–5 конкретных изменений.
3. Результат сохраняется в таблицу `self_learning_suggestions`.
4. Менеджер видит очередь в админке.

**Действия менеджера**:
- **Применить**: изменение автоматически вносится в конфигурацию (текст вопроса, варианты ответов, порядок кнопок). Создаётся A/B-тест (если включён).
- **Отклонить**: предложение архивируется.
- **Изменить вручную**: открывается форма редактирования с предзаполненным текстом, менеджер корректирует и сохраняет.

---

### 4.4. Лог изменений промпта

**UI**: Таблица с историей всех изменений конфигурации бота.

| Дата | Тип изменения | Что изменено | Автор | Конверсия до | Конверсия после | Разница |
|------|---------------|--------------|-------|--------------|-----------------|---------|
| 20.02.2026 | Вопрос воронки | Шаг 3: изменена формулировка | Агент | 68% | 72% | +4% ↑ |
| 15.02.2026 | Описание сегмента | Комфорт: добавлены примеры материалов | Менеджер Иван | 65% | 68% | +3% ↑ |
| 10.02.2026 | Триггер эскалации | Добавлено слово «сомневаюсь» | Админ | 64% | 65% | +1% ↑ |

**Функции**:
- Фильтр по типу изменения.
- Сортировка по дате.
- Клик на строку → детали изменения (diff текстов до/после).

---

### 4.5. Раздел «Белые пятна для заполнения»

**Дублирует функцию из вкладки «База знаний»**, но показывается в контексте самообучения:
- Список вопросов, на которые бот не нашёл ответа.
- Сортировка по частоте (сколько раз задавался одинаковый вопрос).
- Приоритет для заполнения: вопросы, которые задавали 5+ раз.

---

## 5. Раздел: Интеграции

**Назначение**: Настройка подключения к внешним сервисам.

### 5.1. RouterAI

**Поля настройки**:

| Параметр | Тип | Описание |
|----------|-----|----------|
| **API Key** | Текст (скрытый) | Ключ API от RouterAI, получен при регистрации |
| **Основная модель** | Выпадающий список | GPT-4o / Claude 3.5 Sonnet / Gemini 1.5 Pro / Mixtral-8x7B / другие доступные модели. По умолчанию: GPT-4o |
| **Fallback-модель** | Выпадающий список | Резервная модель на случай недоступности основной. По умолчанию: Claude 3.5 Sonnet |
| **Дневной лимит токенов** | Число | Бюджет токенов в день (например, 1 000 000). При достижении лимита бот переключается на fallback или временно отключается |
| **Текущий расход за месяц** | Только чтение | Количество токенов за текущий месяц + стоимость в рублях (если RouterAI предоставляет данные через API) |
| **Webhook для уведомлений о лимите** | URL | Опционально: URL для POST-запроса при достижении 90% лимита |

**Тестирование**:
- Кнопка «Проверить подключение» → отправляет тестовый запрос к RouterAI, показывает статус (успешно / ошибка + код).

---

### 5.2. Telegram Bot API

**Назначение**: Уведомления менеджера о новых лидах + отправка PDF пользователям в Telegram.

**Поля настройки**:

| Параметр | Тип | Описание |
|----------|-----|----------|
| **Bot Token** | Текст (скрытый) | Токен бота, полученный от @BotFather |
| **Chat ID менеджера** | Текст | ID чата, куда отправляются уведомления. Получить можно через @userinfobot |
| **Шаблон уведомления** | Textarea, поддержка переменных | Текст сообщения при новом лиде. Переменные: `{name}`, `{contact}`, `{area}`, `{rooms}`, `{type}`, `{segment}`, `{estimate_min}`, `{estimate_max}`, `{link}` (ссылка на диалог в админке) |

**Пример шаблона**:
```

🆕 Новый лид!

👤 Имя: {name}
📞 Контакт: {contact}

🏠 Квартира: {area} м², {rooms}-комнатная
🔧 Тип: {type}, сегмент: {segment}

💰 Вилка: от {estimate_min} до {estimate_max} руб.

📎 [Открыть диалог](%7Blink%7D)

```

**Кнопки в уведомлении** (настраиваются через inline keyboard Telegram):
- «Позвонить» → ссылка `tel:{contact}`
- «Открыть диалог» → прямая ссылка на детальный просмотр в админке
- «Подключиться к чату» → если пользователь онлайн, открывает интерфейс human takeover прямо из Telegram (через Telegram Mini App или Web App)

**Тестирование**:
- Кнопка «Отправить тестовое уведомление» → отправляет сообщение с тестовыми данными в указанный Chat ID.

---

### 5.3. Яндекс Метрика

**Поля настройки**:

| Параметр | Тип | Описание |
|----------|-----|----------|
| **Номер счётчика** | Число | ID счётчика Яндекс Метрики (например, 12345678) |
| **Список событий** | Чекбоксы | Включить/выключить каждое событие: ☑ `chat_opened`, ☑ `estimate_started`, ☑ `estimate_completed`, ☑ `lead_created` |

**Техническая реализация**:
- На клиенте: вызовы `ym(counter_id, 'reachGoal', 'event_name')`.
- На сервере (дублирование): POST-запросы к Measurement Protocol API Яндекс Метрики (для событий, которые происходят на backend, например, `lead_created`).

---

### 5.4. amoCRM (опционально)

**Назначение**: Автоматическое создание сделок в CRM при появлении нового лида.

**Поля настройки**:

| Параметр | Тип | Описание |
|----------|-----|----------|
| **Webhook URL** | Текст | URL эндпоинта amoCRM для создания сделки |
| **API Key / OAuth токен** | Текст (скрытый) | Токен доступа к amoCRM API |
| **Маппинг полей** | Таблица соответствия | Сопоставление полей из `leads` таблицы с полями amoCRM |

**Пример маппинга**:

| Поле в системе | Поле в amoCRM |
|----------------|---------------|
| `contact_value` | Телефон (ID поля: 123456) |
| `session.utm_source` | Источник (ID поля: 654321) |
| `apartment_params.area` | Площадь квартиры (ID кастомного поля: 789012) |
| `estimate_min` | Бюджет от (ID поля: 345678) |
| `estimate_max` | Бюджет до (ID поля: 345679) |

**Процесс интеграции**:
1. При сохранении лида (создание записи в `leads`) срабатывает webhook.
2. Backend формирует JSON по спецификации amoCRM API v4.
3. Отправляет POST-запрос к `/api/v4/leads`.
4. Статус новой сделки: «Новый лид (AI-чат)» (настраивается в amoCRM как отдельный этап воронки).

**Тестирование**:
- Кнопка «Отправить тестовый лид» → создаёт тестовую сделку в amoCRM.

---

## 6. Дополнительные функции админ-панели

### 6.1. Управление пользователями админки

**Роли**:
- **Администратор**: полный доступ ко всем разделам.
- **Менеджер**: доступ к разделам «Дашборд», «Диалоги» (просмотр + подключение), «Настройка бота» (только чтение).
- **Контент-менеджер**: доступ к разделам «База знаний», «Справочник цен».

**UI**:
- Раздел «Пользователи»: таблица (имя, email, роль, дата регистрации, статус).
- Кнопки: Добавить, Редактировать, Деактивировать.

---

### 6.2. Экспорт данных

**Функции**:
- Экспорт всех диалогов за период в CSV (столбцы: session_id, дата, источник, площадь, тип, сегмент, вилка, контакт, оценка).
- Экспорт лидов в CSV для загрузки в стороннюю CRM.

---

### 6.3. Логи и мониторинг

**UI**: Раздел «Логи».

**Вкладки**:
- **Ошибки LLM**: список неуспешных запросов к RouterAI (код ошибки, время, текст промпта).
- **Ошибки интеграций**: проблемы с Telegram Bot API, amoCRM, Яндекс Метрикой.
- **Системные события**: запуск/остановка сервера, изменения конфигурации.

---

## 7. Технические требования

### 7.1. Технологический стек

| Компонент | Технология |
|-----------|------------|
| **Frontend админки** | React + TypeScript + Tailwind CSS + React Router |
| **Backend API** | Node.js (Fastify) или Python (FastAPI) |
| **База данных** | PostgreSQL (таблицы: sessions, messages, leads, price_matrix, segments, knowledge_gaps, self_learning_suggestions, admin_users) |
| **Авторизация** | JWT-токены, HTTP-only cookies |
| **Real-time обновления** | WebSocket (для статуса онлайн, обновления дашборда) |

### 7.2. Безопасность

- HTTPS обязательно.
- CORS: только для домена приложения.
- Rate limiting для API админки.
- Двухфакторная аутентификация (2FA) для администраторов.
- Хранение паролей: bcrypt (cost factor 12).

### 7.3. Производительность

- Дашборд: кеширование метрик на 5 минут (Redis).
- Список диалогов: пагинация обязательна.
- Индексы БД на поля: `sessions.created_at`, `sessions.status`, `leads.created_at`, `messages.session_id`.

---

## 8. Схема базы данных (ключевые таблицы для админки)

```sql
-- Таблица сессий
CREATE TABLE sessions (
    id UUID PRIMARY KEY,
    created_at TIMESTAMP DEFAULT NOW(),
    utm_source VARCHAR(255),
    utm_medium VARCHAR(255),
    utm_campaign VARCHAR(255),
    device VARCHAR(50),
    status VARCHAR(50), -- chat_opened, estimate_started, estimate_completed, converted, dropped_at_step_N
    message_count INT DEFAULT 0,
    is_online BOOLEAN DEFAULT FALSE,
    last_activity TIMESTAMP
);

-- Таблица сообщений
CREATE TABLE messages (
    id BIGSERIAL PRIMARY KEY,
    session_id UUID REFERENCES sessions(id),
    role VARCHAR(20), -- user / bot / manager
    content TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица лидов
CREATE TABLE leads (
    id BIGSERIAL PRIMARY KEY,
    session_id UUID REFERENCES sessions(id),
    contact_type VARCHAR(20), -- telegram / whatsapp / email
    contact_value VARCHAR(255),
    apartment_params JSONB, -- {area, rooms, type, segment, ...}
    estimate_min NUMERIC(10,2),
    estimate_max NUMERIC(10,2),
    manual_rating VARCHAR(10), -- good / bad / needs_improvement / repeat
    created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица справочника цен
CREATE TABLE price_matrix (
    id SERIAL PRIMARY KEY,
    work_type_id INT REFERENCES work_types(id),
    segment VARCHAR(20), -- econom / standard / comfort / premium
    price_min NUMERIC(8,2),
    price_max NUMERIC(8,2),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица сегментов ремонта
CREATE TABLE segments (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50), -- Эконом, Стандарт, Комфорт, Премиум
    description TEXT,
    what_included TEXT,
    price_range_min NUMERIC(8,2),
    price_range_max NUMERIC(8,2),
    typical_materials TEXT
);

-- Таблица белых пятен
CREATE TABLE knowledge_gaps (
    id SERIAL PRIMARY KEY,
    question TEXT,
    session_id UUID REFERENCES sessions(id),
    created_at TIMESTAMP DEFAULT NOW(),
    filled BOOLEAN DEFAULT FALSE
);

-- Таблица предложений самообучения
CREATE TABLE self_learning_suggestions (
    id SERIAL PRIMARY KEY,
    type VARCHAR(50), -- high_dropout / low_cta_conversion / frequent_unanswered
    problem_description TEXT,
    recommendation TEXT,
    justification TEXT,
    status VARCHAR(20), -- pending / applied / rejected
    created_at TIMESTAMP DEFAULT NOW(),
    applied_by INT REFERENCES admin_users(id),
    applied_at TIMESTAMP
);

-- Таблица пользователей админки
CREATE TABLE admin_users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE,
    password_hash VARCHAR(255),
    role VARCHAR(20), -- admin / manager / content_manager
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW()
);
```


---

## 9. API эндпоинты админ-панели (примеры)

| Метод | Путь | Описание |
| :-- | :-- | :-- |
| `GET` | `/api/admin/dashboard/metrics` | Получить метрики для дашборда |
| `GET` | `/api/admin/dialogs` | Список диалогов (с пагинацией и фильтрами) |
| `GET` | `/api/admin/dialogs/:id` | Детальный просмотр диалога |
| `POST` | `/api/admin/dialogs/:id/takeover` | Подключиться к диалогу (human takeover) |
| `PUT` | `/api/admin/dialogs/:id/rating` | Оценить диалог (✅ / ❌ / ⚠️) |
| `GET` | `/api/admin/bot/personality` | Получить настройки личности бота |
| `PUT` | `/api/admin/bot/personality` | Обновить настройки личности |
| `GET` | `/api/admin/bot/segments` | Получить описания сегментов ремонта |
| `PUT` | `/api/admin/bot/segments/:id` | Обновить описание сегмента |
| `GET` | `/api/admin/bot/knowledge_base` | Список документов и статей |
| `POST` | `/api/admin/bot/knowledge_base/upload` | Загрузить документ |
| `DELETE` | `/api/admin/bot/knowledge_base/:id` | Удалить документ |
| `GET` | `/api/admin/bot/knowledge_gaps` | Список белых пятен |
| `POST` | `/api/admin/bot/knowledge_gaps/:id/fill` | Заполнить ответ на белое пятно |
| `GET` | `/api/admin/prices` | Получить справочник цен |
| `PUT` | `/api/admin/prices/:id` | Обновить цену (inline) |
| `POST` | `/api/admin/prices` | Добавить вид работы |
| `GET` | `/api/admin/self_learning/suggestions` | Очередь предложений от LLM-агента |
| `POST` | `/api/admin/self_learning/suggestions/:id/apply` | Применить предложение |
| `POST` | `/api/admin/self_learning/suggestions/:id/reject` | Отклонить предложение |
| `GET` | `/api/admin/self_learning/changelog` | Лог изменений промпта |
| `GET` | `/api/admin/integrations` | Получить настройки интеграций |
| `PUT` | `/api/admin/integrations/:service` | Обновить настройки интеграции (routerai / telegram / yandex_metrica / amocrm) |


---

## 10. Процессы и автоматизация

### 10.1. Еженедельный анализ самообучения

**Процесс** (запускается через cron):

1. В воскресенье 02:00 MSK запускается скрипт анализа.
2. Выборка диалогов за последнюю неделю из БД.
3. Формирование промпта для LLM-агента с инструкцией анализа.
4. Запрос к RouterAI с контекстом (без передачи персональных данных).
5. Парсинг ответа агента, извлечение предложений.
6. Сохранение предложений в таблицу `self_learning_suggestions`.
7. Уведомление администратору в Telegram: «Новые предложения по самообучению готовы».

### 10.2. Автоматическая переиндексация базы знаний

**Триггер**: при загрузке нового документа или создании/редактировании статьи.

**Процесс**:

1. Файл загружен → статус «Индексируется».
2. Текст извлекается парсером.
3. Chunking (512 токенов, overlap 50).
4. Эмбеддинг каждого chunk через RouterAI.
5. Сохранение векторов в Qdrant/pgvector.
6. Статус «Готово».

### 10.3. Уведомление о достижении лимита токенов

**Триггер**: каждый запрос к RouterAI инкрементирует счётчик токенов в Redis.

**Процесс**:

- При достижении 90% дневного лимита → POST-запрос к webhook (если настроен) + Telegram-уведомление администратору.
- При достижении 100% → переключение на fallback-модель.

---

## Заключение

Данный документ описывает полный функционал админ-панели AI-чат-лендинга для расчёта смет на ремонт квартир. Все функции спроектированы с учётом:

- Удобства использования менеджерами без технических знаний.
- Масштабируемости (поддержка тысяч диалогов в день).
- Безопасности (HTTPS, JWT, rate limiting).
- Интеграции с российскими сервисами (RouterAI, Яндекс Метрика, возможность развёртывания на VPS в РФ).

